// Esta clase tiene la funcion de agregar mayores funcionalidades a las impresiones que se puedan realizar en consola.

class Consola {
    // Instruccion directa de impresion en consola
    static imprimir(mensaje) {
        console.log(mensaje);
    }
}

module.exports = Consola;